[railway]
  builder = "nixpacks"
  language = "node"

[build]
  builder = "nixpacks"
  buildCommand = "cd backend && npm install"
  startCommand = "cd backend && npm start"

[deploy]
  numInstances = 1
  restartPolicyType = "ON_FAILURE"
  restartThreshold = 60
